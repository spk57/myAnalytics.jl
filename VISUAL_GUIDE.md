# myAnalytics Framework - Visual Guide 🎨

## 🎯 The Big Picture

```
                    ┌─────────────────────────┐
                    │   YOUR APPLICATIONS     │
                    │ (Web, Mobile, Desktop)  │
                    └────────────┬────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │   myAnalytics API       │
                    │  (Oxygen.jl Framework)  │
                    └────────────┬────────────┘
                                 │
        ┌────────────────────────┼────────────────────────┐
        │                        │                        │
        ▼                        ▼                        ▼
  ┌──────────────┐        ┌──────────────┐       ┌──────────────┐
  │  Metrics     │        │  Analytics   │       │  Health &    │
  │  Endpoints   │        │  Endpoints   │       │  Version     │
  └──────────────┘        └──────────────┘       └──────────────┘
```

## 📁 Project Structure Map

```
myAnalytics.jl/
│
├─ 📄 README.md ◄──── START HERE! Project overview
│
├─ 📄 QUICK_START.md ◄─ Get running in 2 minutes
│
├─ 📄 API_GUIDE.md ◄─── Complete API reference
│
├─ 📄 ARCHITECTURE.md ◄─ System design & extension
│
├─ 📄 FRAMEWORK_SUMMARY.md ◄─ What's included
│
├─ 📄 VISUAL_GUIDE.md ◄─ This file!
│
├─ Project.toml ◄────── Dependencies (Oxygen.jl, JSON, etc)
│
├─ config.example.jl ◄── Configuration template
│
├─ start_server.jl ◄──── CLI startup script
│
├─ 📁 src/ ◄──────────── Source code
│  │
│  ├─ myAnalytics.jl ◄─ Main module (entry point)
│  │
│  └─ 📁 api/ ◄────────── API implementation
│     │
│     ├─ server.jl ◄───── Server configuration
│     │                     └─ start_server()
│     │                     └─ stop_server()
│     │
│     ├─ routes.jl ◄───── All 8 API endpoints
│     │                     ├─ Health endpoints (2)
│     │                     ├─ Metrics endpoints (3)
│     │                     └─ Analytics endpoints (3)
│     │
│     ├─ utils.jl ◄────── Helper functions
│     │                     ├─ Response/Error structs
│     │                     ├─ Validation functions
│     │                     └─ Utility functions
│     │
│     └─ middleware.jl ◄─ Request processing
│                         ├─ CORS headers
│                         ├─ Rate limiting
│                         ├─ JSON validation
│                         ├─ Authentication
│                         └─ Logging
│
└─ 📁 ssl/ ◄────────── SSL certificates (from persfin)
   │
   ├─ cert.pem ◄─ SSL certificate
   ├─ key.pem ◄─ Private key
   └─ (other SSL files...)
```

## 🔄 Request Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    CLIENT REQUEST                            │
│    GET /api/metrics/cpu_usage                               │
│    POST /api/metrics/query {"metric":"cpu_usage"}           │
│    GET /health                                               │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              OXYGEN.JL ROUTER                                │
│  Match URL pattern to handler function                       │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│         MIDDLEWARE CHAIN (middleware.jl)                     │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 1. ✅ CORS Headers Added                              │  │
│  │ 2. ✅ JSON Body Validated                             │  │
│  │ 3. ✅ Rate Limit Check                                │  │
│  │ 4. ✅ Authentication Check                            │  │
│  │ 5. ✅ Request Logged                                  │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│            ROUTE HANDLER (routes.jl)                         │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ Execute appropriate function:                        │  │
│  │ - Parse request body & parameters                    │  │
│  │ - Call validation (utils.jl)                         │  │
│  │ - Execute business logic                             │  │
│  │ - Format response                                    │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│         RESPONSE CREATED (utils.jl)                         │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ {                                                     │  │
│  │   "status": 200,                                      │  │
│  │   "data": {...},                                      │  │
│  │   "timestamp": "2024-01-01T10:00:00.000"            │  │
│  │ }                                                     │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    CLIENT RESPONSE                           │
│    HTTP 200 OK                                               │
│    Content-Type: application/json                            │
│    {...JSON response body...}                                │
└─────────────────────────────────────────────────────────────┘
```

## 🎛️ API Endpoints Map

```
┌────────────────────────────────────────────────────────────────┐
│                    API ENDPOINTS (8 total)                      │
├────────────────────────────────────────────────────────────────┤
│                                                                 │
│  HEALTH & INFO (2 endpoints)                                   │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ GET  /health                 Server status              │  │
│  │ GET  /api/version            API version info           │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  METRICS (3 endpoints)                                         │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ GET  /api/metrics/:metric_name    Get single metric     │  │
│  │ GET  /api/metrics/list            List all metrics      │  │
│  │ POST /api/metrics/query           Query w/ filters      │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ANALYTICS (3 endpoints)                                       │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ GET  /api/analytics/summary           Summary stats     │  │
│  │ GET  /api/analytics/timeseries/:metric Time series      │  │
│  │ POST /api/analytics/aggregate         Aggregate data    │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
└────────────────────────────────────────────────────────────────┘
```

## 🛠️ Module Responsibilities

```
┌─────────────────────────────────────────────────────────────┐
│                 myAnalytics.jl (Main Module)                 │
│          Orchestrates all submodules and exports API         │
└──────────────────────────┬──────────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ▼                  ▼                  ▼
   ┌─────────┐        ┌────────┐        ┌──────────┐
   │ server  │        │ routes │        │  utils   │
   │  .jl    │        │  .jl   │        │   .jl    │
   └────┬────┘        └────┬───┘        └────┬─────┘
        │                  │                 │
        │  start/stop      │ handle          │ validate
        │  configure       │ requests        │ format
        │                  │                 │ helpers
        │                  │                 │
        └──────────┬───────┴────────┬────────┘
                   │                │
                   ▼                ▼
        ┌──────────────────────────────────┐
        │   middleware.jl                  │
        │  CORS, Auth, Rate Limit, Logging │
        └──────────────────────────────────┘
```

## 📊 Data Flow for GET Request

```
Client: GET /api/metrics/cpu_usage
  │
  ├─ ┌─────────────────────────────┐
  │  │ Oxygen Router               │
  │  │ Match: /api/metrics/:name   │
  │  │ Extract: name="cpu_usage"   │
  │  └──────────┬──────────────────┘
  │             │
  │             ▼
  │  ┌─────────────────────────────┐
  │  │ Middleware Chain            │
  │  │ • Add CORS headers          │
  │  │ • Log request               │
  │  │ • Check rate limit          │
  │  └──────────┬──────────────────┘
  │             │
  │             ▼
  │  ┌─────────────────────────────┐
  │  │ Route Handler (routes.jl)   │
  │  │ @get "/api/metrics/:metric" │
  │  │ function(metric_name)       │
  │  │   data = Dict(...)          │
  │  │   return response           │
  │  └──────────┬──────────────────┘
  │             │
  │             ▼
  │  ┌─────────────────────────────┐
  │  │ Response Created            │
  │  │ Status: 200                 │
  │  │ Body: JSON with data        │
  │  └──────────┬──────────────────┘
  │             │
  └─────────────┴────────────────────────┐
                │                        │
                ▼                        │
  Response: HTTP 200 OK ◄────────────────┘
  {
    "status": 200,
    "data": {
      "metric": "cpu_usage",
      "value": 45.3,
      "timestamp": "2024-01-01T10:00:00.000"
    }
  }
```

## 🔄 Data Flow for POST Request

```
Client: POST /api/metrics/query
        Body: {"metric": "cpu_usage", "filters": {...}}
  │
  ├─ ┌─────────────────────────────────┐
  │  │ Body Received & Buffered        │
  │  └──────────────┬──────────────────┘
  │                 │
  │                 ▼
  │  ┌─────────────────────────────────┐
  │  │ Middleware: JSON Validation     │
  │  │ Parse body                      │
  │  │ Check Content-Type              │
  │  │ Validate JSON structure         │
  │  └──────────────┬──────────────────┘
  │                 │
  │                 ▼
  │  ┌─────────────────────────────────┐
  │  │ Middleware: Rate Limit Check    │
  │  │ Check IP requests count         │
  │  │ Reject if over limit (429)      │
  │  └──────────────┬──────────────────┘
  │                 │
  │                 ▼
  │  ┌─────────────────────────────────┐
  │  │ Route Handler (routes.jl)       │
  │  │ @post "/api/metrics/query"      │
  │  │ body = @json                    │
  │  │ metric = body[:metric]          │
  │  │ filters = body[:filters]        │
  │  │ result = query_logic(...)       │
  │  │ return response                 │
  │  └──────────────┬──────────────────┘
  │                 │
  │                 ▼
  │  ┌─────────────────────────────────┐
  │  │ Response Created                │
  │  │ Status: 200                     │
  │  │ Headers: Content-Type, CORS     │
  │  │ Body: JSON result               │
  │  └──────────────┬──────────────────┘
  │                 │
  └─────────────────┴──────────────────┐
                                       │
                        Response: HTTP 200 OK ◄───┘
                        {
                          "status": 200,
                          "data": {...result...},
                          "message": "Query executed..."
                        }
```

## 🔐 Security Layers

```
                     ┌──────────────────┐
                     │  Client Request  │
                     └────────┬─────────┘
                              │
                              ▼
                    ┌──────────────────────┐
                    │  SSL/TLS Transport   │ Layer 1
                    │  (Optional HTTPS)    │ Encryption
                    └────────┬─────────────┘
                              │
                              ▼
                    ┌──────────────────────┐
                    │ CORS Header Check    │ Layer 2
                    │ (Origin validation)  │ Access Control
                    └────────┬─────────────┘
                              │
                              ▼
                    ┌──────────────────────┐
                    │ API Key Check        │ Layer 3
                    │ (X-API-Key header)   │ Authentication
                    └────────┬─────────────┘
                              │
                              ▼
                    ┌──────────────────────┐
                    │ Rate Limit Check     │ Layer 4
                    │ (Per-IP limiting)    │ DDoS Protection
                    └────────┬─────────────┘
                              │
                              ▼
                    ┌──────────────────────┐
                    │ JSON Validation      │ Layer 5
                    │ (Structure check)    │ Input Validation
                    └────────┬─────────────┘
                              │
                              ▼
                    ┌──────────────────────┐
                    │ Input Sanitization   │ Layer 6
                    │ (SQL injection prev) │ Data Safety
                    └────────┬─────────────┘
                              │
                              ▼
                    ┌──────────────────────┐
                    │  Route Handler       │
                    │  (Execute logic)     │
                    └──────────────────────┘
```

## 🚀 Startup Flow

```
$ julia start_server.jl 8080
  │
  ├─ Parse arguments
  │  └─ port=8080, host="0.0.0.0"
  │
  ├─ Load Julia modules
  │  └─ using myAnalytics
  │
  ├─ Initialize Oxygen framework
  │  └─ Set up HTTP server
  │
  ├─ Load routes (routes.jl)
  │  └─ Register 8 endpoints
  │
  ├─ Load middleware
  │  └─ Set up CORS, rate limiting, etc.
  │
  └─ Start listening on 0.0.0.0:8080
     │
     ├─ ✅ API is now running!
     │
     └─ Accept incoming requests
        └─ Route to handlers
           └─ Return responses
```

## 📈 Scaling Architecture

```
                        Internet Users
                              │
                    ┌─────────┴─────────┐
                    │                   │
         ┌──────────▼──────────┐  ┌──────────────────┐
         │   Load Balancer     │  │   Failover       │
         │   (nginx, HAProxy)  │  │   (standby)      │
         └──────────┬──────────┘  └──────────────────┘
                    │
        ┌───────────┼───────────┐
        │           │           │
        ▼           ▼           ▼
   ┌────────┐  ┌────────┐  ┌────────┐
   │ API    │  │ API    │  │ API    │
   │ 8080   │  │ 8081   │  │ 8082   │
   └───┬────┘  └───┬────┘  └───┬────┘
       │           │           │
       └───────────┼───────────┘
                   │
       ┌───────────┴───────────┐
       │                       │
       ▼                       ▼
    ┌─────────────┐    ┌────────────┐
    │ Database    │    │ Redis Cache│
    │ PostgreSQL  │    │ (Optional) │
    └─────────────┘    └────────────┘
```

## 🎯 Development Workflow

```
1. SETUP (One time)
   julia> using Pkg
   julia> Pkg.activate(".")
   julia> Pkg.instantiate()

2. START
   julia> using myAnalytics
   julia> start_analytics_api()
   
   ✅ Running on 0.0.0.0:8080

3. TEST
   $ curl http://localhost:8080/health
   $ curl http://localhost:8080/api/metrics/list

4. CUSTOMIZE
   Edit src/api/routes.jl
   Add your custom endpoints
   
5. REDEPLOY
   Ctrl+C to stop
   julia> start_analytics_api()
   
   ✅ Running with your changes

6. DEPLOY
   $ julia start_server.jl 8443 --ssl
   
   ✅ Ready for production!
```

---

**This visual guide helps understand the framework structure and flow. For detailed documentation, see README.md and API_GUIDE.md** 📚

